import React, { memo } from "react";
import { Col, Container, Row } from "react-bootstrap";
import Heading from "../../../elements/Heading";

const Topics = memo(() => {
    return (
        <Container fluid className="px-4" style={{
            minHeight: '100vh',
            width: '100vw',
            backgroundColor: '#000'
        }}>
            <Row>
            </Row>
        </Container>
    )
})

export default Topics;